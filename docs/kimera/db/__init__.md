# Package `kimera.db`

Database layer components:
- `Database`: async SQLAlchemy wrapper for relational connections.
- `DBFactory`: lazy loader for database definitions in configuration files.
- `ResourceRepo` & `AutoWireRepo`: repository patterns for SQL data.
- `MongoDoc`: helpers for flattening MongoEngine documents.
