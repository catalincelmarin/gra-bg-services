# Module `kimera.__init__`

Marks the `kimera` namespace package. The runtime components live in the child packages such as `helpers`, `comm`, `store`, etc. No classes or functions are declared here.
