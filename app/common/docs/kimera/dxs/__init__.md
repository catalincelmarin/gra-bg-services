# Package `kimera.dxs`

DXS (Data eXchange Services) abstractions used to scaffold REST endpoints from YAML configuration. Includes common behaviour for resource-style CRUD endpoints and base plumbing for custom services.
